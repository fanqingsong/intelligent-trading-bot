# Lighter ML deps for pipeline containers (skip tensorflow for faster builds).
# Install full requirements.txt locally if neural-net classifiers are needed.
apscheduler
tqdm
numpy==2.*
pandas==2.*
pyarrow
akshare
dateparser
numba
ta-lib
scikit-learn==1.8.*
lightgbm==4.*
joblib
